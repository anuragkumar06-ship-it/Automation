"""Three-panel geographic locator maps for Cognizant Foundation proposals.

The pipeline is deterministic end to end: the same configuration and the same
data files always produce the same image. No part of it calls a language model
or an image generator, and no boundary is ever drawn, approximated or invented.
"""

__version__ = "0.1.0"
